"use client";

/** Menus live in the Odoo-style top bar — kept as no-op for existing imports. */
export type SalesTab = "bookings" | "invoices" | "products" | "quoteTemplates";

export function SalesSubnav() {
  return null;
}
